<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moirai.alloc.gantt.query.mapper.ProjectMembershipQueryMapper">
    <select id="existsAssignedMember" resultType="boolean">
        SELECT CASE WHEN COUNT(1) > 0 THEN TRUE ELSE FALSE END
        FROM squad_assignment
        WHERE project_id = #{projectId}
          AND user_id = #{userId}
          AND final_decision = 'ASSIGNED'
    </select>
</mapper>
